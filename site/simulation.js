setTimeout(() => {
    console.log(canvas.width);
}, 1000);
count = 0;
function place_loc(longitude, latitude, x, y){
    if (latitude<43.59) {
        if (count == 0){
            
        };
    }
    
}